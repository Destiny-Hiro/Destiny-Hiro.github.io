const Dog = (doggo) => ( 		//Function for HTML component
	`
	<div class="center">
		<img class="animals" src="${doggo.message}">

	`
) 								//Return the Dog url

export default Dog; 			//Export Dog function